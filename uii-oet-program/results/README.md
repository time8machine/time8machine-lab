# Results Policy

Results are kept separate from source code.

A frozen result set must contain the exact source commit, benchmark version, preregistration version, command/configuration, seeds, raw data, processed data, manifest/hash, validation status, and independent audit status.

A CI-generated artifact is not automatically a frozen scientific result. Freezing occurs only after the run is inspected for implementation errors and the exact artifact is intentionally archived or committed.
